# design_analysis

Advanced family wrapper for route selection and `Spec/Plan` work when the task
cannot stay inside a single direct-atomic family.

Use `runtime.py` first. This family exists to resolve route selection when a
deployment or performance request cannot safely stay inside a single atomic
path.
