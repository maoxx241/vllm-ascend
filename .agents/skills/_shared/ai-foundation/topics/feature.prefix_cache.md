---
topic_id: feature.prefix_cache
canonical_term: prefix_cache
topic_kind: feature
---

# Feature: prefix_cache

## Core

- topic_id: `feature.prefix_cache`
- canonical_term: `prefix_cache`
- aliases: `prefix_cache`, `prefix cache`, `prefix-cache`, `前缀缓存`, `开缓存`, `automatic prefix caching`, `复用前缀`

## Foundation

- 该特性属于部署/推理配置能力。

## Deployment View

- 部署时应先检查模型/硬件前置条件，再开启。

## Development View

- 开发时应核验定义-读取-生效的完整证据链。

## Details/Edge Cases

- 与参数 topic 通过 `primary_feature` 关联，所有值语义在参数 topic 中展开。
