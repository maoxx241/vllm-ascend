# deployment-artifact-packager

Atomic deployment skill that packages config, launch script, and minimal
validation notes into the final deployment artifact pack.

If the capsule says `single-card` is undocumented, do not invent a single-card
launch command. Package the documented baseline script instead and call out the
unsupported topology explicitly.
